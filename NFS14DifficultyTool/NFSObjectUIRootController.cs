namespace NFS14DifficultyTool {
    public class NFSObjectUIRootController : NFSObject {
        public NFSObjectUIRootController(MemoryManager memManager)
            : base(memManager, "49035e20b4e797b5196b4686a183a4d4") {
            //foxUIRootController
            //This is pretty much just used to determine that we're loaded into the game, so don't bother with any fields
        }
    }
}
